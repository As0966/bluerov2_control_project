numpy>=1.21.0
matplotlib>=3.5.0
pandas>=1.3.0
scipy>=1.7.0
rich>=12.0.0
torch>=1.10.0  # Required for INN Controller
# For ROS 2 python dependencies
# Ensure these are installed via apt if pip fails
# rclpy, geometry_msgs, nav_msgs
